
{% extends "baseEmploye.html.twig" %}



{% block body %}

    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 >Les catégories de véhicules disponibles</h1>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <div class="col-md-3">
            <p class="lead">Catégorie de véhicule<p>
            <div class="list-group">
                {% for uneCateg in mesCategories %}
                    <a href="{{ path("employe_autopart_default_cataloguebycateg") }}/{{ uneCateg.cat }}" class="list-group-item">{{ uneCateg.lib }}</a>
                {% endfor %}
            </div>
        </div>
    </div>

    <!-- jQuery -->


{%  endblock %}

